{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Pre-commit",
            "type": "shell",
            "command": "pre-commit run --all-files",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Run pre-commit manually."
        }
    ]
}
